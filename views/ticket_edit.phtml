<li class="list-group-item ticket fixed" id="fixed">
    <div class="media">
        <form method="POST" enctype="multipart/form-data">
            <div class="media-left">
                <img class="media-object" src="public/images/<?=htmlentities($ticket['img'])?>" alt="image" style="max-height:60px;max-width:60px;">
                <?=htmlentities($user['login'])?>
                <?=htmlentities($ticket['id'])?>
            </div>
            <div class="media-body">
                <input type="text" name="title" value="<?=htmlentities($ticket['title'])?>"></input>
                <input type="text" name="content" value="<?=htmlentities($ticket['content'])?>"></input>
                <input type="file" name="img" value="<?=htmlentities($ticket['img'])?>"></input>
            </div>
            <div class="ticketform">
                <input type="hidden" name="ticket_id" value="<?=htmlentities($ticket['id'])?>">
                <div class="btn-group btn-group-vertical" role="group">
                    <div class="btn-group" role="group">
                        <button type="submit" class="glyphicon glyphicon-ok js_valid_ticket" value="valid_ticket" name="action"></button>
                    </div>
                    <div class="btn-group" role="group">
                        <button type="submit" class="glyphicon glyphicon-remove js_delete_ticket" value="delete_ticket" name="action"></button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</li>
